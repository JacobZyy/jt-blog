# Asia/Shanghai: every day at 10:00.
# `pnpm content:schedule:install` creates this entry automatically.
CRON_TZ=Asia/Shanghai
0 10 * * * cd /path/to/jt-blog && NOTION_SYNC_ENV_FILE='/path/to/jt-blog.env' '/absolute/path/to/pnpm' content:sync >> /path/to/jt-blog/logs/notion-sync.log 2>&1
